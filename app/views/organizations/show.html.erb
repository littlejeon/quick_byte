<% if current_user.is_member?(@organization) && current_user.is_admin?(@organization) %>
<%= render @organization %>

<%= link_to "Edit Organization", edit_organization_path %>
<% else  %>
"You must be part of this organization to view it"
<%end%>
